#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    double base, exponent, result;
    base = 5;
    exponent = 4;
    result = pow(base, exponent);
    cout << "pow(" << base << "^" << exponent << ") = " << result;
    double x = 25;
    result = sin(x);
    cout << "\nsin(" << x << ") = " << result;
    return 0;
    return 0;
}