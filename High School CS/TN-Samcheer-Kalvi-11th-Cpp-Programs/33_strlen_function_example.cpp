#include <iostream>
#include <string.h>
using namespace std;
int main()
{
    char source[] = "Computer Science";
    cout << "\n Given String is " << source << " its Length is " << strlen(source);
    return 0;
}